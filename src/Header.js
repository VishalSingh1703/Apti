import React from 'react';

const Header = () => {
    return (
        <header className="my-4">
            <h1 className="text-center">Problems on Trains</h1>
        </header>
    );
};

export default Header;
